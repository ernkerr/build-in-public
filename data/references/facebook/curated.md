# Curated references — Facebook (erin.codes Page)

## TODO

No curated FB examples yet. Add real shipped posts here as they accumulate, with a one-line note on *why* each one worked (or didn't).

Format per entry:

```
### Ref A — short label

[full post content]

**Why it landed:** [one line]
**Pattern:** [what structural move it makes — opener, parenthetical, close, etc.]
```

Until references exist, FB drafts default to the direction in `data/voice/facebook/voice.md` (low-confidence — expect to iterate).
